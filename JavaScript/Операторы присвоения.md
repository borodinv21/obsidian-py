Для присвоения какого-либо значения в переменную с одновременным её вычислением воспользуемся следующими конструкциями.

Пример:
```JavaScript
let a = 10
let b = 7

a += b //a = a + b
a -= b //a = a - b
a *= b //a = a * b
a /= b //a = a / b
```