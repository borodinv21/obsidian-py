С помощью данного оператор можно фильтровать данные после группировки [[GROUP BY]].

Сгруппируем студентов по именам и будем выводить только уникальные.
```SQL
SELECT Имя, COUNT(*) FROM Студенты
GROUP BY Имя
HAVING COUNT(*) = 1
```